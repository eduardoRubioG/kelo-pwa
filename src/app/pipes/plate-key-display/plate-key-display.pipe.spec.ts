import { PlateKeyDisplayPipe } from './plate-key-display.pipe';

describe('PlateKeyDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new PlateKeyDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
